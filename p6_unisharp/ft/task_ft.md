# task_ft.md — 把 UniK3D / UniSHARP 深度先验微调到"水乡室外"域

## 0. 一句话目标
UniSHARP 在我们这批航拍全景上"能用但不保真",根因是它的 UniK3D 深度先验训练在
**OmniRooms 室内仿真**(≤100m、有天花板、无天空、≤30cm 位移)。本任务把这个先验
**微调到室外水乡分布**,且**不需要任何深度真值**——用无人机自身在航线上的真实平移
做光度新视角监督。

相关上下游:`[[unisharp]]` 方法解剖见对话;数据来自 `data/8kpano/scenes/scene_NNNhf_pano/`,
位姿来自 `p3_pano/pano_cams_scene_NNNhf.json`(VGGT,240 帧/场景)。

---

## 1. 为什么这次"零视差"不挡路(和之前 3DGS 失败的区别)
- 之前 3DGS 失败 = **优化内三角化**需要同一机位的同步基线,我们没有 → 乱麻。
- 本任务 = **微调前馈先验**,监督方式不同:
  `source 帧 → 预测高斯 → 渲染到 target 位姿 → 对齐真实 target 帧(RGB+感知)`。
  无人机沿航线**真实平移**,真带基线的 (source,target) 对要多少有多少 → 合法监督。
- 深度真值不需要:UniK3D 自己跑一遍当**伪深度弱锚**(`aux_depth_scale_loss`),
  真正拉动微调的是光度损失。这正是仓库里 `datasets/re10k.py`(真实视频、无 GT)
  已有的配方;全景版模板是 `datasets/sim_panorama.py`。

---

## 2. 代码事实依据(已逐一核对,避免拍脑袋)
| 事实 | 出处 |
|---|---|
| 监督 = 光度新视角(RGB+ResNet50 感知)+ UniK3D 伪深度尺度锚 | `losses/unisharp_loss.py` `_ResNet50Perceptual`;`cli/unified_trainer.py` `_aux_ray_losses` |
| 真实视频路子已存在:自动用 UniK3D 生成伪深度,远景按阈值置 0 | `datasets/re10k.py` `pseudo_depth_autogen / pseudo_far_depth_invalid_m=30` |
| 全景训练对是**固定朝向、纯平移**(`_SimFrame` 只有 `position_xyz`) | `datasets/sim_panorama.py:143-148` |
| 全景 dataset 布局:scene_dir 内 rgb + 含"depth"路径的深度文件 + `pose_root/<scene>.csv` | `sim_panorama.py:_scan_scene_frames / _parse_pose_csv` |
| 深度头可训(`requires_grad_(True)`),位置=半径×射线、网络只出有界残差 | `models/unisharp_feature.py:UniK3DCopiedDepthHead`;`models/gaussian_composer.py:_forward_mean` |
| UniK3D 全景深度 API:`infer(rgb, camera=Spherical([fx,fy,cx,cy,W,H,π,π/2]))` | `UniK3D/unik3d/models/unik3d.py:284`;`utils/camera.py:346` |

---

## 3. 命门与风险(按严重度,A 档先验证可行性)
1. **相对位姿质量(最致命)**:光度监督的位姿一偏,梯度全错。
   - 缓解:只用**短帧距**对(相邻 1–8 帧,VGGT 相对位姿比全局重建可靠得多);
     `pano_cams` 的 `R_wp/C` 已现成。**`.insv` 内无 GPS、无 ffmpeg 可见 IMU**(实测:
     双鱼眼 HEVC + mov_text 字幕,无 gpmd)→ 度量位姿路堵死,只能用 VGGT。
2. **尺度一致性**:UniK3D 深度是**米制**,VGGT 的 `C` 是**任意尺度**。位移必须换算到米,
   否则光度损失里"相机移动了多远"与深度不在同一单位 → 系统性错位。
   - 缓解:`--pos-scale` 把 VGGT 单位拉到米。估法:`scale ≈ median(UniK3D 半径距离) /
     median(VGGT 点云到相机距离)`,或用已知地标高度(~15m)反推。A 档先给经验值并在
     验收里检查。
3. **天空 / 远景**:室外几十~几百米,远超室内 30m。
   - 缓解:伪深度里把天空像素置 0(=无效),`far_depth_invalid_m` 抬到 ~150m。
4. **基线参数**:`pair_max_translation_m=0.5` 是室内默认,航拍是米级 → 放大
   (更大的真实基线 = 更强监督,这次是好事)。
5. **过拟合/灾难遗忘**:数据量小,**先 A(只调 heads)/ B(+LoRA),不要直接全量 C**。

---

## 4. 路线 A → B → C
| 档 | 解冻 | 数据量 | 何时上 |
|---|---|---|---|
| **A 只调 heads** | 冻结 UniK3D,只训 `DirectPredictionHead`+`UniK3DCopiedDepthHead` | 1–2 场景、几百对 | **现在,验证域微调是否有效** |
| **B +LoRA 编码器** | A + `pixel_encoder` 加 LoRA | 3–5 场景 | A 见效后 |
| **C 全量微调 UniK3D** | 整个 backbone | 全部场景 + 增广 | 仅当 B 仍受限且数据够 |

---

## 5. A 档数据构造管线(本次已落地)
脚本在 `p6_unisharp/ft/`,产物在 `p6_unisharp/ft/data/`(已 gitignore)。

```
原始: data/8kpano/scenes/scene_NNNhf_pano/panoramas/pano_*.jpg  (240 ERP/场景)
      p3_pano/pano_cams_scene_NNNhf.json                        (R_wp, C / 帧)
  │
  ├─[1] derotate_and_pose.py  按 R_wp 把每帧 ERP 去旋转到统一世界朝向(纯平移残差)
  │        → ft/data/<scene>/NNNNN.jpg        (canonical ERP)
  │        → ft/poses/<scene>.csv             (frame,x,y,z = C*pos_scale, 米)
  │
  ├─[2] pseudo_depth.py       UniK3D(vitl) 在 canonical ERP 上出米制径向深度
  │        + 天空置 0、远景 >far 置 0
  │        → ft/data/<scene>/depth/NNNNN.npy  (float32 H×W, 0=无效)
  │        → ft/data/<scene>/sky/NNNNN.png    (mask, 供 B 档光度降权)
  │
  └─[3] make_pairs.py         按平移幅度[min,max]+帧距筛 (src,tgt) 对
           → ft/pairs/<scene>.jsonl
```
布局即 `SimPanorama` 约定:`root=ft/data, pose_root=ft/poses, scene=<scene>`。

运行:`bash p6_unisharp/ft/build_ft_dataset.sh 027 023`

### 验收(数据档)——027 全量已过
- [x] canonical ERP:027 出 **240 帧** + pose.csv;叠加 100/104/108 三帧,地平线不散
      → 去旋转朝向一致 ✓(拖影来自真实平移基线,符合预期);
- [x] pairs:813 对 / 229 帧有配对(pos_scale=1.0);
- [x] 伪深度:240 帧离线跑通,目视**地面近(蓝)/地平线远(红)/天空黑**,物理合理 ✓;
- [⚠] 但 **valid 跨帧 0.11–0.98、med 3.3–287m 抖动大** → UniK3D 在此 OOD 序列不稳
      (正是微调要修的);A 档靠光度兜底、伪深度仅弱锚。

### 5.2 重力对齐(B 档,已实现)
去旋转后地平线呈正弦波 → canonical 帧对齐到 **VGGT 世界系而非重力垂直**(VGGT 系不水平
+ 无人机横滚)。UniSHARP 训练在**正立 ERP**,倾斜 ERP 是额外 OOD。
`derotate_and_pose.py --gravity-level` 已加重力校平,左乘全局 `R_level`(同一旋转,不破坏
帧间纯平移),两种估"上"法:
- **`--level-method horizon`(默认,推荐)**:把每帧 sky/ground 边界拟合到倾斜大圆
  `sin(lon)nx+tan(lat)ny+cos(lon)nz=0`(齐次 LS),pano 系法向 →`R_wp^T`→ 世界"上",多帧平均。
- `--level-method trajectory`:相机轨迹 PCA 最小轴;**仅平飞有效,banked footage 失败**
  (实测 027 给出 ≈+Y 的错误估计,地平线没拉平)。

**实测(027)**:horizon 法 12 帧一致性 `spread=0.005`,修正 ~8°,亮带从"骑在赤道上方/斜穿"
变为**对称居中于赤道**。残留弯曲是高空斜俯视的真实几何(远景在 ERP 本就弯),非全局旋转可消。
B 档重建数据:`GRAVITY=1 bash build_ft_dataset.sh 027`(rgb 旋转后伪深度需一并重算)。

### 5.1 冒烟实测发现(027,2 帧)——两个真实结论
1. **伪深度严重 OOD**:UniK3D 把整景压在 **56–150m**(med 84–134m),且 far=150 截断后
   **只剩 16–35% 有效**。这正面印证"室内先验在室外失真"——但也说明**伪深度只能当很弱的
   尺度锚,真信号必须靠光度**。调整:`FAR_INVALID_M` 抬到 **~300**(留住更多远景),
   A 档训练时把 `aux_depth_scale_loss_weight` 调小。
2. **尺度修正(推翻先前猜测)**:相邻帧位移 med≈0.20 VGGT 单位,×30fps≈**6 单位/秒**——
   与无人机 ~6m/s 巡航吻合,说明 **VGGT 单位已近似米,`POS_SCALE≈1` 即可**,不要用先前
   "≈10"的值(会把基线灌大 10 倍 → 光度监督全错)。**严谨估法**:取一对,用光流视差
   `disparity ≈ B/Z` + UniK3D 的 Z 反解 B,与 `||C_tgt-C_src||·pos_scale` 对齐定标
   (留作 step3.5,A 档先用 1.0)。
3. 推论:基线 0.2–1m / 深度 ~100m → B/Z 很小,远景光度信号弱。**对策**:训练对偏向
   `--max-gap` 大端取更大基线;近景(地面/建筑)才是主要监督来源,远景靠伪深度弱锚兜底。

---

## 6. A 档训练(已落地并跑通)
`train_a.py`(launcher)+ `train_a.sh`(wrapper)驱动**现成 CLI**,无需另写 trainer。
直接复用 `SimPanorama`(我们的数据布局就是按它造的)。
```
bash p6_unisharp/ft/train_a.sh 027              # STEPS/LR0/UNIK3D_LR0/... 见脚本头
```
**关键:stock CLI 没有 resume,且训练分支的 UniK3D 离线加载有 bug**。launcher 用三处
干净 monkeypatch 解决(不改 gitignore 的 clone):
1. **加载预训练权重**:模型构造后 `load_from_checkpoint(INIT_CKPT, strict=False)`
   → 实测 `missing=0 unexpected=1`,确是**从预训练微调**而非从零;
2. **离线 UniK3D**:adapter 的 `UniK3DHub(config_variant="eval")` 在官方 clone 上必抛
   TypeError 且 offline 时拒绝回退 → patch `unik3d_adapter.load_unik3d_model` 直接走
   `UniK3D.from_pretrained`(缓存,离线);
3. **配对/尺度**:SimPanorama 写死 `pair_max_translation_m=0.5`、`position_scale=0.01`
   (室内默认)→ 经 env 改 `SIM_PAIR_MAX_TR=6`、`SIM_PAIR_MIN_OVERLAP=0.1`、
   **`position_scale=1.0`**(否则米制位移被缩 100×、与米制伪深度单位不一致 → 0 对)。

**冻结策略**(纯靠 CLI 分组 LR,无需 patch):`--unik3d-encoder-lr0/1 0` 冻 DINOv2 编码器;
`--unik3d-lr0/1 1e-5` 轻调 UniK3D 解码器+深度头(**即"微调深度先验"**);`--lr0 1e-4` 训 heads。

**冒烟实测(027,8 步)**:`dataset=sim` 采到对、loss 9.0–9.8 无 NaN、tgt(新视角)loss≈2.7、
~1.1s/步(gsplat 扩展首次编译 ~187s)。✅ 端到端可训。

## 6.1 正式训练 + 留出评测(023,已完成)✅
完整管线已跑通并验证微调**确实涨点**。
1. `make_holdout.py --scene scene_023hf --val-frac 0.2` → train 192 帧 / val 48 帧(symlink,不复制);
2. `make_val_manifest.py --scene scene_023hf_val` → 验证用显式配对 manifest(`scene|src|tgt,..`);
3. 训练:`SC_NAME=scene_023hf_train STEPS=2000 ... train_a.sh 023`
   → loss **8.67→2.46**,tgt(新视角)**2.01→1.77**,存档 step_{500..2000}.pt;
4. 评测:`eval_a.py`(=`run_validation` + 同款 monkeypatch),留出 48 对,微调前 vs step_2000:

| | PSNR | SSIM |
|---|---|---|
| 微调前(pretrained) | 18.713 | 0.6231 |
| 微调后(step_2000)  | **19.121** | **0.6275** |
| 增量 | **+0.41 dB** | **+0.0044** |

**结论:A 档域微调在留出帧上确有正向提升(非过拟合)**。幅度温和符合 A 档预期
(冻结编码器、单场景 2k 步、伪深度/VGGT 位姿有噪声、8° 重力残差)。

### 评测踩坑(已固化)
- `run_validation` 的 sim manifest 是 **`scene|src|tgt1,tgt2,..`** 三段式,不是场景名列表;
- `_load_model` 用 `strict=True` 载入,预训练 ckpt 多 `payload.depth_alignment` → eval_a.py 强制 strict=False;
- 渲染路径有**偶发异步 CUDA 错误**(同帧单独跑正常)→ eval_a.sh 固定 `CUDA_LAUNCH_BLOCKING=1`(慢但稳);
- 验证 SimPanorama 同样写死 `pair_max_translation_m=0.5/position_scale=0.01`,且原生 ERP 分辨率 → monkeypatch 改米制 + `max_long_edge=512`。

### 验收(微调档)
- [x] 留出帧新视角 **PSNR/SSIM** 微调后优于微调前(+0.41dB / +0.0044);
- [ ] LPIPS(blocking 下太慢,暂用 FAST 跳过;后续单跑一次全指标);
- [ ] 深度直方图从"~150m 轨道"展开(待 B 档);
- [ ] 大位移 novel view 鬼影/拉花减少(对比 `[[unisharp]]` 当前 027 fused)。

## 6.2 B 档(多场景 + 解冻编码器)— 已做,结论:不及预期
`MANIFEST_SCENES="scene_023hf_train 021 022 025 026 028"`(均重力校平)+ `ENC_LR0=1.5e-6`
(解冻 DINOv2 编码器)+ 3000 步。同一 023 留出 48 对评测:

| 配置 | PSNR | SSIM |
|---|---|---|
| 基线(pretrained) | 18.713 | 0.6231 |
| A 档(023 单场景 2k,**冻结**编码器) | **19.121** | 0.6275 |
| B 档(6 场景 3k,**解冻**编码器) | 19.111 | **0.6318** |

**结论:多场景+解冻编码器没有在 PSNR 上超过 A 档**(19.11≈19.12,噪声内),仅 SSIM +0.0043。
解读:评测只在 023,训练别的场景对 023 留出帧帮助有限;~19.1 dB 像是当前数据质量
(伪深度噪声/VGGT 位姿/重力残差)的天花板,A、B 都触顶。**真正的杠杆在数据质量与
单场景特化,不在模型容量**。`train_a.sh` 已支持 `MANIFEST_SCENES`/`ENC_LR0` 复现。

## 6.3 长训练(023 单场景 6000 步)— 步数 vs 指标曲线
逐 checkpoint 在同一 023 留出 48 对评测(曲线 `runs/long_curve.png`):

| 步数 | PSNR | SSIM |
|---|---|---|
| 0(baseline) | 18.713 | 0.6231 |
| 1500 | 19.127 | **0.6360**(SSIM 峰) |
| 3000 | 19.275 | 0.6317 |
| 4500 | 19.251 | 0.6288 |
| 6000 | **19.295** | 0.6301 |

**结论:增益主要在前 1500 步拿到(+0.41dB,SSIM 最高);之后 PSNR 缓爬到 +0.58dB
但 SSIM 单调下滑 → 轻度过拟合。最佳折中 ≈ step 1500。A/B 档点都落在同一平台
→ ~19.3 dB 是数据质量天花板,与步数/场景数/编码器无关。**

## 6.4 全指标(含 LPIPS)— 重要修正:感知质量退化
对 baseline 与 long step_3000 跑完整指标(去 `--fast`):

| | PSNR ↑ | SSIM ↑ | **LPIPS ↓** |
|---|---|---|---|
| pretrained | 18.713 | 0.6231 | **0.4665** |
| ft step_3000 | 19.275 | 0.6317 | **0.4987** |

**⚠ LPIPS 变差(0.4665→0.4987),尽管 PSNR/SSIM 升。** 即微调把输出推向更贴合
像素亮度/低层结构,却牺牲了感知真实感(更平滑/模糊)——光度+L2 监督的典型副作用。
**净质量混合偏负:不能只看 PSNR 宣称"变好"。** LPIPS 是最接近人眼的指标,它退化说明
当前 A 档配方(强光度、弱噪声伪深度锚)对"看起来更真"并无帮助,甚至有害。
**修正建议:提高 `--lambda-percep`、降 `--lambda-color`,或改善伪深度/位姿质量;
单纯调步数/场景/编码器都救不了感知质量(§6.2/6.3 已证天花板)。**

## 6.5 深度直方图(预测高斯径向距离)— 因果链闭合
对 023 留出帧 00200,预训练 vs 微调 step_3000 各出 ply,直方图 `‖xyz‖`
(`depth_hist.sh`/`depth_hist.py`,图 `runs/depth_hist_00200.png`):

| | 中位 | p10–p90 | <50m |
|---|---|---|---|
| pretrained | **2.1m** | 1.3–10.2 | 100% |
| ft step_3000 | **98.5m** | 80.8–99.4 | 4% |

**预训练把整景压在 1–10m(OmniRooms 室内先验,尺度完全错);微调把它推到室外
~100m 尺度——这正是本任务的几何目标,且解释了 PSNR/SSIM 提升(尺度对了,重投影更准)。
但微调把深度塌缩成 ~100m 的窄壳层(p10–p90 仅 80–99m,几乎无相对结构),因为喂的
UniK3D 伪深度本身就平(§5.1:OOD 下 med~100m、低方差)。壳层几何 = 视差细节丢失 =
LPIPS 退化。**

## 7. 总结论(四项实验闭环)
1. **域微调确实把深度先验从室内 ~2m 纠正到室外 ~100m**(几何目标达成),带来 +0.4–0.6dB
   PSNR / +SSIM 的留出增益,且非过拟合(留出帧)。
2. **但感知质量(LPIPS)退化**,根因是伪深度锚本身平坦 → 微调学到一个 ~100m 窄壳层,
   丢了相对深度结构。
3. **天花板在数据/监督质量,不在模型容量**:多场景、解冻编码器、6000 步都触同一 ~19.3dB
   平台;step 1500 即近最优。
4. **下一步唯一有效杠杆 = 提升监督质量**:更好的伪深度(多视一致/单目-视频深度而非单帧
   UniK3D)、真实稀疏深度锚、或重权感知损失;而非继续堆步数/场景/参数。
全部脚本(`derotate_and_pose`/`pseudo_depth`/`make_pairs`/`build_ft_dataset`/`make_holdout`/
`make_val_manifest`/`train_a`/`eval_a`/`depth_hist`)均已落地、跑通、可复现。

## 8. 视频单目深度(VDA)替代 UniK3D 伪深度 — 已做
按 §7 唯一杠杆("提升监督质量")验证:用 **Video-Depth-Anything (vitl, relative)** 对
023 去旋转 ERP 序列出**时序一致深度**,替代平坦的 UniK3D 单帧伪深度。
脚本 `ft/vda_depth.py`(VDA 克隆在 `p6_unisharp/vda/`,已 gitignore,需重 clone+下权重;
直接喂 240 帧、disparity→depth、sky 自动定向、全局尺度→中位 100m、far>300 置 0)。

**深度锚质量(VDA vs UniK3D):**
| 锚 | valid | 中位 | p10–p90 跨度 |
|---|---|---|---|
| UniK3D 单帧 | 0.30–0.55 | ~100m | ~19m(平) |
| VDA 视频 | **0.93–1.00** | ~100m | **127–173m(有结构)** |

**重训 023(单场景 2k,冻结编码器,VDA 深度)→ 同一留出 48 对:**
| 配置 | PSNR↑ | SSIM↑ | LPIPS↓ |
|---|---|---|---|
| baseline | 18.713 | 0.6231 | 0.4665 |
| UniK3D-ft | 19.275 | 0.6317 | 0.4987 ❌ |
| **VDA-ft** | 18.793 | 0.6127 | **0.4686** ✓ |

**预测深度(VDA-ft):中位 80.6m,p10–p90 = 42–98m(跨度 56m,~3× UniK3D-ft 的壳层),
未塌缩。**

### 结论(权衡翻转,非干净的赢)
- **VDA 修复了感知退化**:LPIPS 0.4987→0.4686(≈baseline),证实"平坦锚→塌缩→LPIPS 退化"
  的因果,结构化锚消除了它。
- **但丢了 PSNR/SSIM 增益**(18.79/0.613,SSIM 还跌破 baseline)。
- 机制:**近零基线下 PSNR 只奖励"平均尺度对"**(UniK3D 平壳层即可),**LPIPS 奖励结构真实**。
  VDA 在 ERP 上几何近似(透视模型 + 2:1 + 极点畸变)→ 有结构但每像素不够精确 → 像素指标平。
- **两种锚互补缺陷,都非全面提升**。干净的赢需要"几何正确 + 结构化 + 度量对齐"的深度:
  **VDA 经 cubemap 分面(每面方形/透视,无 ERP 畸变)+ 用 UniK3D 度量做逐面仿射对齐**,
  兼得 VDA 的结构与 UniK3D 的尺度精度。这是下一步。
- 更根本:**真正的瓶颈仍是缺真实几何**——零视差航拍 + 无 GPS/IMU。最大杠杆其实在
  **采集端**(`[[unisharp]]` 飞行规划:加基线/GPS),而非深度后处理。

## 9. 语义先验 + 几何水面平面(LocateAnything / CLIPSeg)— 已做,决定性阴性
按用户建议"用 LocateAnything 先验提升深度精度"。
- **GPS 判定**:exiftool 查 023 的 .insv/.prx = **0 个 GPS 标签(没录 GPS)**,仅 15.6MB
  Insta360 IMU trailer(陀螺/加速度,无绝对高度)。→ 无绝对尺度来源。
- **LocateAnything-3B 闸门:失败**。航拍 ERP 上只给整块粗框(water=整幅、sky=顶半)+ 重复
  artifact,无法精细定位水面(俯视+等距柱状完全 OOD)。`la_detect.py` 存档。
- **改用 CLIPSeg(CLIP 文本→像素分割):闸门通过**。水掩膜准确勾出湖面,sky/植被/建筑合理。
  `clipseg_mask.py`。
- **几何水面平面**(`fuse_waterplane.py`):重力校平系下水像素 depth=h/(−sin lat)(几何精确),
  其余用 VDA,天空置 0;全局拟合 h=**33m**(物理合理的无人机离水高度,自洽性 OK)。

**重训 023(VDA+水面平面)→ 同一留出 48 对:**
| 配置 | PSNR | SSIM | LPIPS |
|---|---|---|---|
| baseline | 18.713 | 0.6231 | 0.4665 |
| UniK3D-ft | 19.275 | 0.6317 | 0.4987 |
| VDA-ft | 18.793 | 0.6127 | 0.4686 |
| **VDA+水面平面** | 18.779 | 0.6079 | 0.4717 |

### 决定性结论(整条深度-监督研究线闭合)
**几何精确的深度(h=33m 恢复、水面平面精确)没有传导到新视角质量——与 VDA-ft 持平。**
四种深度锚(平坦度量 UniK3D / 结构相对 VDA / 结构+几何精确水面平面 / 语义)全部落在
**~18.7–19.3 PSNR、~0.61–0.63 SSIM、~0.47–0.50 LPIPS** 的同一平台,无一干净取胜。

**根因(最终、可执行):不是深度监督质量,而是零基线采集**。近零视差下,深度再准也几乎
不改变小基线新视角的重投影(深度对 NVS 不敏感),所以任何深度先验(度量/结构/几何/语义)
都撬不动这个 NVS 指标。**深度图本身确实变好了(可用于更大基线视角或网格导出),但对当前
近基线 NVS 无用。**
→ **唯一有效杠杆确认在采集端**:`[[unisharp]]` 飞行规划加真实基线(+GPS 给绝对尺度)。
后处理侧的深度工程到此已探到边际,结论稳健。

---

## 7. 当前状态
- [x] 可行性论证 + 代码核对 + 风险定级(本文件)
- [x] A 档数据脚本:`derotate_and_pose.py / pseudo_depth.py / make_pairs.py / build_ft_dataset.sh`
- [ ] 跑 027/023 生成数据并过"数据档验收"
- [ ] A 档训练封装 + 冻结策略 + 留出评测

## 10. 目标#2:物体摄影测量度量 + 多帧融合(扩漫游范围)— 已探,采集再次封顶
用户目标从"成片好看"转向"真实尺度 + 多帧度量融合扩大可漫游范围/消重影"。

### 真正的新进展(值得记)
- **微调模型给出正确尺度的 per-frame 几何**:对 023 原始全景用微调 ckpt 推理,高斯径向
  距离 median **74.9m**(p10–p90 43–98m),**不再是预训练的 ~2m 塌缩**(§6.5)。这正是
  之前 "027 fused" 重影的根因——当时用预训练,是一堆 2m 气泡壳在叠。融合终于有了正确基础。
- **融合尺度定标**:pano_cams 相机基线 60↔120 = 0.445 VGGT 单位。旧融合用 `--scale 30`
  → 隐含 ~160 m/s 飞行(荒谬)→ **scale=30 正是重影主因**;物理自洽的 **s≈1**(VGGT≈米)。

### 但撞到更硬的采集墙:轨迹几乎没有空间跨度
- 023 **整条轨迹 bbox 对角线 = 1.948 VGGT 单位**;最分散的 4 帧(1/73/82/85)两两也只
  ~0.6–1.2 单位。→ **整个拍摄的相机运动挤在 ~2 单位小盒里(无人机兜小圈/近悬停)**。
- 若 s≈1:~2m 位移 / ~75m 深度 → B/D≈0.02,**近零视差**,所有帧近乎同视点 → 融合只能
  加冗余,**扩不了覆盖/漫游范围**。
- 若 VGGT 压缩了真实平移(已知失败模式):真实基线也许更大,**但没有度量参照就无法确定**。

### 摄影测量度量为何在此也失效
- 定融合尺度/确认基线需要**独立度量参照**;云自洽(Chamfer)对尺度是**退化的**(s→0 平凡最优),
  必须外部锚。
- 无 GPS(§9 已证 .insv/.prx 零 GPS);本park 场景**缺可靠已知尺寸物体**(民居/景观非标准,
  LA 也框不准);VGGT 尺度不可信。→ 绝对度量与真实基线**均欠定**。

### 目标#2 结论
**微调模型修好了"per-frame 尺度"这一真问题(实质进展),但多帧融合扩漫游范围仍被采集封顶
——这次不是深度质量,而是轨迹几乎没有空间跨度 + 无度量参照。** 与 §7–§9 一致:
**后处理侧已全面探底,唯一杠杆在采集端**——飞行规划需刻意拉开基线(几十米级位移)并记录
GPS/已知标尺,`[[unisharp]]` 的量化飞行模型正是为此。

## 11. 转折点:.insv trailer 里有真实 GPS+IMU,VGGT 位姿才是真凶(023)
用户提示 .insv 有"仪表/飞行数据"。调查结论:
- 画面无烧录 OSD(双鱼眼+.prx 均纯图);飞行数据在 **Insta360/Antigravity v3 二进制 trailer**
  (UUID `8db4..26bf`,"Antigravity A1")。exiftool 13.59 / Gyroflow telemetry-parser 都**解不出**
  A1 变体。**手动解析偏移索引表**成功。
- **GPS(record id=7,53B/样本,238 全 fix=A)**:上海水乡 31.168°N,121.638°E;
  **范围 370×358×108m,航迹 1763m**。**Euler(id=14)实为 30Hz 单位四元数**(11407 样本)= 每帧姿态。
- **VGGT 是垃圾**:VGGT 轨迹与 GPS Umeyama Sim3 对齐残差**中位 102m**(≈场景 30%),形状都不符。

### GPS+四元数 → 6DoF 米制位姿 → 重训对比 VGGT(决定性)
`build_gps_poses.py`(GPS→ENU 位置 + 四元数→R_wp;全局偏移无害,只需相对位姿),
米制 init 点云(VGGT init 在错帧),`run_pano_gsplat_train.sh` 同参重训:

| | VGGT 位姿 | GPS+IMU 位姿 |
|---|---|---|
| 重建 bbox(5–95%) | **0.9×0.3×0.9 单位(乱麻)** | **561×546×157 m(真实米制)** |
| PSNR / SSIM / LPIPS | 18.88 / 0.613 / 0.469 | 18.91 / **0.654** / 0.481 |
| 几何 | 塌成一团 | 连贯水乡布局(topdown 可见湖岸/路/建筑) |

**同全景、同训练器,唯一变量=位姿。** held-out pano PSNR 相近(两者都能过拟合邻近视角),但
**几何天差地别**——GPS 产出真实 600m 尺度连贯 3D,VGGT 塌成 1 单位。

### 全项目结论(修正 §7–§10)
之前"重建失败/零基线/采集是墙"的判断**部分被推翻**:采集**有充足基线**(1763m 航迹、
109m 垂直),真凶是 **VGGT 位姿估计在航拍 360 上崩溃**。**杠杆其实一直在手边——用 .insv
自带的 GPS+IMU 位姿替换 VGGT**。深度先验那一串(§6–§9)仍成立但已非主线;主线是位姿。

### 待办(高价值)
- [ ] 验证四元数约定(航向 cos 偏低,慢速 GPS track 噪声致;当前经验判定 loss 收敛=约定基本对)
- [ ] 用打了 EQUIRECT 补丁的 gsplat 出 GPS vs VGGT 的正规全景渲染对比
- [ ] 通用化提取器覆盖全部场景 → 全部用 GPS+IMU 位姿重训
- [ ] GPS 有绝对尺度 → 度量融合/漫游、真实测量全部解锁(§10 目标#2 现可达)

## 12. 朝向求解:混合位姿全指标取胜(023,+2.4dB)
四元数在另一机体轴系,共轭搜索(24 perm)失败 → 双常量 `R_wp=A·M(q)·B`;
GPS 航向 Procrustes 失败(环绕飞行,镜头≠航迹向)→ **无假设解法**:地平线拟合给每帧
pano 系 up `n_i`,交替解 A 与 `u'=B·up`(quat_solve3),残差 10°;时间偏移排除(±3s 全平)。
最后 **混合位姿**(quat_hybrid/gps_hybrid_scene):倾角逐帧吸附到地平线实测、偏航保留解基。

| 023 位姿 | PSNR | SSIM | LPIPS | 留出渲染误差 |
|---|---|---|---|---|
| VGGT | 18.88 | 0.6132 | 0.4688 | 16.0/255 |
| GPS 错基 | 18.91 | 0.654 | 0.481 | 23.5/255 |
| GPS 解基(10°) | 19.38 | 0.6601 | 0.4778 | — |
| **GPS 混合** | **21.32** | **0.669** | **0.4626** | **13.2/255** |

**朝向质量每提一步指标单调涨一步;混合位姿首次三指标齐超基线(+2.4dB)——
整条深度线求而不得的"干净的赢"由位姿修正实现。** 留出渲染仍偏糊:剩余瓶颈=偏航精度/
GPS 插值(1.6s)/卷帘 → 下一杠杆是训练内位姿微调(BA)。A、B 是相机安装常量,跨场景复用。

## 13. 全 7 场景 GPS混合 vs VGGT 终表(同 15k iter 等额预算)
| 场景 | VGGT (PSNR/SSIM/LPIPS) | GPS混合 | ΔPSNR |
|---|---|---|---|
| 021 | 22.62/0.775/0.427 | 22.20/0.749/**0.400** | −0.42 |
| 022 | 23.72/0.778/0.414 | 23.05/0.753/**0.383** | −0.68 |
| 023 | 18.94/0.678/0.467 | **20.78**/0.664/0.467 | **+1.84** |
| 025 | 21.08/0.717/0.431 | 20.92/0.682/0.431 | −0.17 |
| 026 | 22.30/0.692/0.466 | 21.37/0.666/0.480 | −0.92 |
| 027 | 20.01/0.648/0.509 | **20.09**/0.617/0.526 | +0.09 |
| 028 | 21.32/0.634/0.552 | **22.04**/0.615/0.564 | **+0.72** |

诚实结论:
1. **held-out pano 指标上两者总体持平**(GPS 3胜4负,均值≈0;LPIPS 021/022 显著更优)。
   注:GPS@7k 反而普遍优于 @15k(21.32 vs 20.78 on 023)——长训过拟合,各取最优时 GPS 3 胜。
2. **但该指标看不见根本差异**:VGGT 重建是 <1 单位塌缩团块(靠过拟合插值刷分),GPS 是
   **数百米米制连贯几何**(027 达 1065×124×1386m)。视角插值指标对"几何是否真实"不敏感
   (§9 同一教训)。GPS 位姿的真价值在插值之外:**可漫游、可融合、可测量、可导出**。
3. VGGT 崩得最狠的 023 上 GPS +1.8~2.4dB;VGGT 局部一致性最好的 021/022/026 上略负
   ——残余偏航/GPS插值误差 vs VGGT 局部光度自洽的权衡。**训练内位姿微调(BA)可两者兼得,
   是明确的下一杠杆。**
4. 管线跨 7 场景零失败(horizon fits 全 240/240,trailer 自动解析全过)。

## 14. 训练内位姿微调(BA)— 两轮实测:自由BA崩、锚定BA持平
`train_pano_gsplat_sph.py` 加 `POSE_OPT=1`:每相机 so3 旋转增量+平移增量随光度联合优化
(run_pano_gsplat_train.sh 转发 POSE_* env)。

| 场景 | 无BA (PSNR) | BA自由 v1 | BA锚定 v2 | Δ(v2−无BA) |
|---|---|---|---|---|
| 021 | 22.43 | (中断) | 22.39 | −0.05 |
| 022 | 23.07 | 21.71 | **23.20** | +0.13 |
| 023 | 21.32 | 20.74 | **21.34** | +0.02 |
| 025 | 20.99 | 20.39 | 20.93 | −0.06 |
| 026 | 21.42 | 19.84 | 21.34 | −0.08 |
| 027 | 20.71 | 19.15 | 20.62 | −0.09 |
| 028 | 22.32 | 21.75 | 22.22 | −0.10 |

结论:
1. **自由 BA(v1)全线 −0.6~−1.6dB**:教科书规范漂移——train 位姿+场景一起漂,test 相机
   冻在 GPS 基准 → 留出评测崩。
2. **锚定 BA(v2,σ_r=2°/σ_t=2m 高斯先验+LR÷10)修复了回退但只到持平**(均值 −0.03,
   022/023 微涨):先验夹太紧位姿基本没动。
3. **窗口很窄**:自由=漂移,紧锚=不动。真正的收益需要规范固定的 BA(锚定少数相机固定
   规范、或 eval 时对 test 位姿做 Sim3 对齐再评)+ 更松先验——留作后续;当前诚实结论:
   **BA 在本实现下不亏不赚,GPS 混合位姿(§12)仍是最佳已验证配置**。

## 15. 规范固定 BA(v3)— 持平收口:瓶颈在 test 位姿噪声,非 train 位姿
`POSE_GAUGE=1`:每步后对 train 相机增量做零均值投影(锁全局 6 自由度,不特权相机)
+ 弱先验(REG=1e-3)+ LR 恢复(r=1e-3,t=1e-2)。

| 场景 | 无BA | v1自由 | v2锚定 | v3规范固定 | Δ(v3−无BA) |
|---|---|---|---|---|---|
| 021 | 22.43 | — | 22.39 | **22.48** | +0.05 |
| 022 | 23.07 | 21.71 | **23.20** | 23.09 | +0.02 |
| 023 | **21.32** | 20.74 | 21.34 | 21.08 | −0.24 |
| 025 | 20.99 | 20.39 | 20.93 | **21.03** | +0.04 |
| 026 | 21.42 | 19.84 | 21.34 | 21.40 | −0.02 |
| 027 | 20.71 | 19.15 | 20.62 | 20.64 | −0.07 |
| 028 | 22.32 | 21.75 | 22.22 | 22.22 | −0.10 |

结论(三轮 BA 完整收口):
1. v3 消除了 v1 的漂移回退且位姿真在动(LR 大 10 倍无崩),但均值仍 ≈0(−0.05)。
2. **三轮变体夹逼出真相:留出指标的剩余误差不由 train 位姿主导,而由 test 相机自身的
   GPS/IMU 位姿噪声主导**——train 侧 BA 修不了 test 侧噪声,而动 test 位姿即污染评测。
   这是"测量受限"的天花板,与模型/优化无关。
3. BA 的真实收益(几何更干净、漫游更稳)不体现在此指标;需渲染巡检或独立几何验证。
   **最佳配置维持:GPS 混合位姿,BA 可选(v3 无害)。** 全部旋钮已入库:
   `POSE_OPT/POSE_GAUGE/POSE_LR_R/POSE_LR_T/POSE_REG/POSE_START`。

### §15.1 结论3的进一步验证(渲染巡检)— 结果:不支持"BA有隐藏收益"
同一留出视角渲染 无BA vs BA-v3(021/023,test2):
- 渲染误差与指标表完全同向:021 12.2→12.1(持平),023 21.9→24.8(更差);
- 目视:BA3 与无BA 形态几乎一致(地平线带略更整,雾感相同),无浮子减少/锐化。

**修正结论 3:BA 在渲染层面同样无可见收益。** 当前留出视角的"雾"是共同上限:
全部位姿(含 test)带 GPS/IMU 噪声 + 240 全景覆盖 ~600m 场景的密度不足。
BA 三变体 + 渲染巡检共同证明:**位姿后处理已到测量精度天花板;要更清晰,
需 RTK 级位姿或更密集采集**(回到采集端,与 §7 一致但这次机制明确)。

## 16. 密化验证:023 从 240→1140 张(密度那一半天花板确认可修)
240 是 VGGT O(N²) 时代的天花板;GPS+IMU 位姿在任意帧时刻插值(gps=238 采样,
fits 1140/1140),密度约束消失。同 7k iter、同协议(cams[::8] 留出):

| 023 | PSNR | SSIM | LPIPS |
|---|---|---|---|
| 240 张 | 21.32 | 0.669 | 0.463 |
| **1140 张** | **22.34** | **0.691** | **0.460** |

**+1.02dB、三指标齐升**(且留出集更大 143 vs 30,评测更严)。累计:VGGT 18.94 →
GPS 混合 21.32 → 密化 22.34 = **+3.4dB 全指标**。§15.1 天花板两半中"密度"一半
已修;剩余为 GPS/IMU 噪声(需 RTK)。查看器新增 023×1140 按钮(gps_023dense.ksplat)。

## 17. 密化收口:饱和点 ~1140,全 7 场景普涨(均 +1.13dB)
- 饱和:023 240→1140 = +1.02dB,1140→3799 = 仅 +0.13dB → **1140 是甜点**。
- 全场景(1140 张,GPS 混合,7k):021 23.81 / 022 23.96 / 023 22.34 / 025 22.45 /
  026 22.28 / 027 22.29 / 028 23.05 —— 对 240 版 **7/7 全胜,均 +1.13dB**(027 最大 +1.58)。
- `batch_dense.sh` 一键复现;剩余天花板 = GPS/IMU 噪声(RTK 方向)。

## 18. 密化+BA(v3)— 密度不解锁 BA
023dense(1140)+ POSE_GAUGE=1:22.25/0.690/0.461 vs 无BA 22.34/0.691/0.460(−0.08dB)。
与 §15 一致:**任何密度下 BA 均持平微负,test 位姿噪声是唯一剩余墙(RTK)**。
密度线与 BA 线双双收口;最佳配置 = GPS 混合 + 1140 张 + 无 BA。

## 19. LingBot-Map 对比(023,240 前向 crop,与 VGGT 同输入)
蚂蚁 Robbyant 流式前馈 3D 重建(GCT,ETH3D F1=98.98)。streaming + --use_sdpa
(绕过 FlashInfer),SAVE_NPZ 补丁存位姿。GPS 真值 Umeyama Sim3 裁判:

| 位姿源 | Sim3 残差中位 | 场景范围参照 |
|---|---|---|
| VGGT | 101.9 m | 370×358×108 m |
| **LingBot-Map** | **101.0 m** | 同 |
| GPS+IMU | ≈0(真值) | — |

**结论:LingBot-Map 在航拍 360 上与 VGGT 同样彻底失败**(残差≈场景 30%,两者几乎同值
——同一 DINOv2/518px 前馈范式的共同 OOD:俯拍+大尺度+等距柱状 crop)。室内/桌面基准的
SOTA 排名对这类数据没有迁移性。**GPS+IMU 位姿仍是唯一可行方案**;前馈位姿模型在此域
需要专门训练数据,不是换模型能解决的。

## 20. 抽帧终审:雾=位姿噪声,变量已全部排除
023dense @2048 分辨率+激进增密:N 122k→195k,SSIM 0.691→0.705,LPIPS 0.460→0.453,
但抽帧与 1024 版**视觉几乎相同(仍雾)**,渲染误差 11.3≈11.4。
至此所有变量已受控排除:密度(240→1140→3800)、分辨率(1024→2048)、高斯数、BA 三变体
——雾不动。**结论闭合:模糊由跨视角位姿错位主导(GPS±2m + 偏航~1° ≈ 5–10px 错位,
多视共识=平均=雾),单张输入锐是因为它不需要共识。唯一出路=厘米级位姿(RTK 采集,
或 GPS 先验+特征级 SfM 精化)。**

## 21. GPS 先验 + COLMAP 特征级 BA(023)— 持平;"位姿噪声墙"需修正
管线:GPS 位姿→COLMAP 先验模型→SIFT+序列匹配→point_triangulator→自由 BA→Sim3 回对齐
GPS(scale=0.994,**BA 修正量 med=1.44m/p90=9.24m,恰为预测的 GPS 噪声量级**)→重训。
结果:**21.41/0.670/0.465 vs 基线 21.32/0.669/0.463 —— 持平**。
关键:这次 **train+test 全部 240 相机都被特征级精化**(240/240),§15 的"test 位姿噪声"
借口不再成立,但雾仍在。
### 修正后的最终归因(候选)
位姿修正 1.4m 已做、分辨率/密度/数量/BA 全排除 → 剩余雾的主导因素很可能是
**场景非刚性 + 拼接几何**:023 大面积是**水面(波纹逐帧变)与摇动植被**——静态场景假设
对这些像素根本不成立,任何位姿精度都救不了;另有双鱼眼 blend=average 的无视差拼接鬼影。
可检验预测:建筑/道路区域应比水面/树冠清晰得多(局部对比可证)。
**工程结论:该数据的 NVS 质量已到数据本身(动态内容+拼接)的上限;
再往上要么静态场景重拍,要么按语义分治(水面用平面先验替代重建,§9 工具现成)。**

## 22. 局部审计证伪"非刚性主因":雾是全局的
CLIPSeg 分区测锐度保持率(grad_render/grad_GT):水 11.4% / 植被 11.5% /
**建筑 8.2% / 道路 3.6%** —— 刚性区域不比水面清晰,全图仅保住 4–12% 梯度能量。
§21 的非刚性归因**被证伪**。
### 排除法收敛后的头号嫌疑(唯一横跨全图、且位姿 BA 修不了的量)
**拼接/相机模型的图内角度畸变**:v360 双鱼眼按 ih_fov=200 的名义参数映射,若鱼眼真实
FOV/畸变与名义有 0.5–1° 偏差,ERP 上就有随位置变化的系统性角误差——等效"每像素位姿误差",
跨视角(不同 yaw)不一致 → 全局雾;BA 只能修外参,修不了这种内参/拼接畸变。
### 可检验(下一步):绕开拼接
直接用**原始单鱼眼**做标定+COLMAP+透视 3DGS(不经 stitch),若锐度显著恢复即坐实;
或对鱼眼做真实标定(棋盘/自标定)重新拼接。至此:位姿线、密度线、BA 线、非刚性线全部
排除,**拼接标定是最后一个未审计的系统组件**。

## 23. 终验:绕开 stitch 的单鱼眼直通链路——拼接畸变假设坐实 ✅
原始下视鱼眼 240 帧(1920²)→ COLMAP OPENCV_FISHEYE 自标定 SfM(model1 注册 240/240,
f=548.1,反推 FOV=200.7° 正中名义 200°;model0 为 2 帧假起点碎片)→ 去畸变 → INRIA 3DGS 7k:

| 链路 | test PSNR | **锐度保持率** |
|---|---|---|
| stitched pano(最佳 dense) | 22.34 | **4–12%(雾)** |
| **单鱼眼直通** | **24.37** | **42.7%** |

**全局雾的真凶 = v360 名义参数(ih_fov=200)拼接的图内角度畸变**,锐度差 4–10 倍。
§20–§22 的排除法(位姿/密度/分辨率/BA/非刚性全持平)正确收敛到了这里。
### 修复路径(按工程量)
1. **直接双鱼眼流水线**(已验证):两路鱼眼各自 COLMAP 刚性 rig + 3DGS,弃 ERP 训练;
2. 用自标定参数(f=548.1@1920, k1..k4)替换 v360 名义映射**重拼 ERP**,复用全部现有管线;
3. GPS+IMU 位姿仍有价值:给 COLMAP 做先验/尺度(§21 管线现成)。

## 24. 标定重拼 ERP:单帧质量佳,但位姿链路未收敛(开放问题)
recal_stitch.py(OPENCV_FISHEYE 正向模型,f=548.1/k1..k4)单帧 ERP 预览锐利干净,
远胜 v360。但重训崩(15.4–16.1):地平线-四元数残差 19°(v360 pano 为 10°)。
已排除:上镜头 roll(重叠环对齐 corr=0.767,真值 180/−1;原 288/+1 是天空噪声假解)、
左右镜像(0.755>0.730)、双流时间同步(环 corr 0.75–0.85 稳定)。
**未解**:自制 ERP 与机体系的旋转关系仍与 v360 存在非纯旋转差异(疑 phi/手性约定,低分辨率
相关测试不足以定夺)。
### 工程决定
- **生产路径 = 鱼眼直通管线**(§23 已证:24.37 PSNR / 42.7% 锐度,无需 ERP);
- ERP 重拼留作开放项:正确做法是 COLMAP **双镜头刚性 rig 标定**(解出镜头间旋转),
  而非相关性启发式;
- 全部工具(recal_stitch cal/calup/synctest/batch)入库可复现。

## 25. rig 标定 ERP 链路:锐度突破(4–12%→33.9%),遗留三项工程尾巴
路线:上视 SfM 碎裂(最大 76 帧、R_rig spread 80°→不可用)→ **光度法解 R_rig**
(重叠环 4 帧 corr=0.850)+ down-SfM 特征位姿(Sim3 保内部一致)+ **训练内核约定坐实**
(读 gsplat_equirect_kernel.patch:pano 系 y-down,py=(lat/(π/2)+1)H/2——此前三连崩
的真凶就是 y-up/y-down 图像空间镜像,旋转不可拟合)。
**结果**:渲染首次出现清晰场景(道路/运河/建筑),**下 75% 区锐度保持率 33.9%**
(v360 仅 4–12%,接近鱼眼直通 42.7%)。PSNR 12.9 失真是 GT 缺陷所致,非几何:
1. **天顶黑洞+机身残影**(up 镜头天顶映射缺陷;R_rig 倾角 a=−3° 撞到搜索边界,需扩窗);
2. **双流曝光接缝**(上下镜头独立 AE,需增益补偿);
3. render-GT 有恒定俯仰偏移(常量 pano 系旋转残差,可由 R_rig 扩窗或 A-solve 吸收)。
### 状态:主干打通,收尾三项均为已定位的工程活
工具链全部入库:rig_solve(SfM 位姿+GPS 定标)/rrig_photo(光度 R_rig)/stitch3(内核约定拼接)。
